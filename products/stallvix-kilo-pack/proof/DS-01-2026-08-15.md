# Work Receipt — DS-01 policy/runtime-root hardening

- `packet_id`: SVX-GRA-CUR-DS-01-POLICY-RUNTIME-ROOT-HARDENING (PR #73 packet, DeepSeek F2 + F3 control-gap, F4/F7 regression, Gate-D contract)
- `executor`: Claude Code session (packet-allocated to Cursor; executed by agent lane, sequential rail)
- `capability_grant`: packet allowed source paths only — `products/stallvix-kilo-pack/{kilo.jsonc, AGENTS.md, packets/P2-FULL-TEST-A-CLOSEOUT.md, PAYLOAD-MANIFEST.json}`, `scripts/stallvix-kilo-pack.test.mjs`, one source audit receipt (this file)
- `branch`: `audit/svx-gra-ds-01-policy-runtime`
- `baseline_sha`: `c981abf2185908eb122ea29152ab04d2978044cb` (PR #6 head, verified as worktree HEAD)
- `ending_sha`: this commit (created after this receipt; contains this receipt)
- `pushed`: yes
- `pull_request`: none (review via branch; STOP before merge per P10)

## Result by acceptance ID

| ID | Result | Evidence |
| --- | --- | --- |
| P1 | PASS | `kilo.jsonc` implementer `edit` has no `src/**`/`docs/**` allow; catch-all `"*": "ask"` first, hard-denies last; prompt no longer claims src/docs edit focus |
| P2 | PASS | edit hard-denies SPEC/SCOPE/BACKLOG/AGENTS/CLAUDE/AGENTS.granaide-kilo.md, `docs/agent-work/packets/**`, `docs/audit/**`, `.kilo/**`, `.kilo-runtime-data/**`, migrations/wrangler/env/pem/key/`*.env` — asserted by P1/P2 tests |
| P3 | PASS | investigator unchanged safe default: `edit`/`bash`/`grep`/`external_directory` deny; launcher-binding test still passes |
| P4 | PASS (source policy; runtime proof deferred) | read + glob deny `.kilo-runtime-data/**` for base and both agents; no runtime-root exception in source; mechanical runtime proof left to Gate C/D |
| P5 | PASS | deep-path vectors (2–3 nested levels: credentials.json, server.pem, private.key, .env.production) deny under read + edit via same matcher semantics |
| P6 | PASS | compound/prefixed bash negatives (`&&`, `;`, `| cat`, `cmd /c`, powershell-wrapped, `cd . &&`) resolve deny; no runtime proof claimed |
| P7 | PASS | Gate-D contract split into two independent probes: job-grant narrowing + authority immutability on packet paths |
| P8 | PASS | `PAYLOAD-MANIFEST.json` regenerated via committed manifest script (CRLF→LF normalize); old hashes superseded (see table) |
| P9 | PASS | test 11/11, lint 0, build 0, `git diff --check` 0 |
| P10 | PASS | no consumer sync, no merge, no authenticated Kilo, no promotion; commit + push only |

## Payload hashes (before → after)

| File | Before | After |
| --- | --- | --- |
| `kilo.jsonc` | `edd7365a982935aa269e024e5a6f0a96cba9ff1a714f084f84eba9f7cedf6dab` | `8c3bd8de4aea2c145bc4318294771adca2af8d39664277b23ec3f660d1b7f10d` |
| `AGENTS.md` | `67baa8d1060dce1165a1b611c2295fbea9caabd8b30134f87597148c668bfe96` | `c2242b7f1706b2dc749ee0deb6001dffcca3a0d40346b30ec4073bfff38db543` |
| `run-stallvix-kilo.ps1`, 3 skills | unchanged | unchanged |

Manifest normalized (UTF-8, strip BOM, CRLF→LF); source files kept CRLF/UTF-8-no-BOM.

## Changed paths

- `products/stallvix-kilo-pack/kilo.jsonc` — F2/F3 policy hardening (Problem A + B)
- `products/stallvix-kilo-pack/AGENTS.md` — capability table + two new boundary paragraphs (F2/F3)
- `products/stallvix-kilo-pack/packets/P2-FULL-TEST-A-CLOSEOUT.md` — Gate-D bounded-mutation contract
- `products/stallvix-kilo-pack/PAYLOAD-MANIFEST.json` — regenerated (hash supersede)
- `scripts/stallvix-kilo-pack.test.mjs` — F4 deep paths, F3 opacity, F7 compound shells, P1/P2 asserts
- `products/stallvix-kilo-pack/proof/DS-01-2026-08-15.md` — this receipt

No other tracked or untracked files (node_modules untracked/ignored; no package.json/package-lock changes).

## Effective policy summary

- **read / glob (base + both agents):** catch-all allow, `.kilo-runtime-data/**` deny last.
- **edit (implementer):** catch-all `"*": "ask"`; NO unconditional src/docs allow; hard denies: authority/evidence paths, `.kilo/**`, `.kilo-runtime-data/**`, `supabase/migrations/**`, `wrangler.toml`, `.env`/`.env.*`/`*.env`/`*.env.*`, credentials/pem/key shapes; `supabase/functions/**` lowered from allow to ask.
- **investigator:** unchanged — edit/bash/grep/external_directory deny; read/glob safe-default with runtime-root deny.
- **bash (implementer):** unchanged default-deny with narrow verification allowlist; new F7 negatives statically resolve deny.
- **implementer prompt:** now states edits are limited to exact job-packet grants; no unconditional src/docs focus.

## Commands run

| Command | Exit | Result |
| --- | ---: | --- |
| `node --test scripts/stallvix-kilo-pack.test.mjs` | 0 | **11/11 pass**, 0 fail/skip (incl. launcher worktree-binding, manifest, P1/P2/F3/F4/F7 cases) |
| `npm run lint` | 0 | eslint clean |
| `npm run build` | 0 | Next.js 16.3.0 production compile + TypeScript pass |
| `git diff --check` | 0 | no whitespace errors |
| `git status --short` | 0 | exactly the 5 expected modified files before receipt |
| `npm install` | 0 | 22 packages, 0 vulnerabilities (network step, no dependency changes committed) |

## Working tree

- clean after commit (receipt included); node_modules untracked and ignored.

## Remaining uncertainty

- P4/P6 runtime mechanical proof (Kilo 7.4.20 effective-permission and live shell behavior) still deferred to Gate C/D after credential clearance — source tests do not fake runtime proof.
- F1/F5-F10 and the P2 authenticated negative probes remain open (later packets per PR #73 rail).
- `jsonc-parser` devDep exists but the test still uses its own strip+JSON.parse — F9 replacement is DS-02's job.
- Context7 MCP review/removal is DS-02, not this packet.

## Reusable lesson found

- Kilo rule ordering (last-match-wins) plus per-tool boundaries means containment must be specified per tool (`read` ≠ `glob` ≠ `grep` ≠ `edit` ≠ `bash`), and unconditional broad edit allows (`src/**`, `docs/**`) are the single most dangerous shape for a self-authorizing executor pack — a job-grant model is the safe steady state.