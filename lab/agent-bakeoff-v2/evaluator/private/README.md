# Evaluator-private surface

This directory is **not** a candidate evidence root.

Adapters must not copy, mount, search, or embed these files into a candidate-input bundle.

Committed goldens exist so the public Granaide harness can score later candidates. Adapter isolation is mechanical: allowed fixture roots exclude `evaluator/private`. A human reading this git repository can still open these files; that is a publication limitation, not adapter access.

Do not place StallVix private document bodies here either. Goldens refer only to synthetic Northwind Harbor facts.
